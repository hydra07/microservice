export default async () => {
  // const session = getSession();
  // console.log('Session:', session);
  return (
    <div>
      <h1>Test Page</h1>
      {/* <p>Session: {session}</p> */}
    </div>
  );
};
