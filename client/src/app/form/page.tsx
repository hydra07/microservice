'use client'
import React from 'react';
import Form from '@/components/ui.custom/user/formfood';

const HomePage: React.FC = () => {
    return (
        <div>
            <Form />
        </div>
    );
};

export default HomePage;
