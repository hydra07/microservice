'use client'
import React from 'react';
import Form from '@/components/ui/formfood';

const HomePage: React.FC = () => {
    return (
        <div>
            <Form />
        </div>
    );
};

export default HomePage;
